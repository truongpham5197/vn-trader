DATABASE_URL="file:./dev.db"
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
# false = tắt bot polling — dùng khi share token với app khác (vd Hermes);
# alert vẫn gửi bình thường, chỉ mất commands + nút inline
TELEGRAM_POLLING="true"
TCBS_API_KEY=""
NAV_VND="500000000"
RISK_PCT="0.01"
PAPER_TRADING="true"
KILL_SWITCH="false"
# Mật khẩu đăng nhập web để chỉnh cấu hình (bỏ trống → dùng CRON_SECRET)
ADMIN_PASSWORD=
